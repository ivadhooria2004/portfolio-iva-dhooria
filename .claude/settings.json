{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; git add \"$f\" && git commit -m \"Update $(basename \"$f\")\" && git push origin main; } 2>/dev/null || true",
            "statusMessage": "Committing and pushing changes..."
          }
        ]
      }
    ]
  },
  "sandbox": {
    "filesystem": {
      "allowRead": [
        "~/Downloads"
      ]
    }
  }
}
